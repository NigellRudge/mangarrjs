export enum RecordStatus {
  archived = "archived",
  active = "active",
}

export enum Role {
  admin = "admin",
  user = "user",
}

export enum MangaMonitorStatus {
  new_chapters = "new_chapters",
  none = "none",
  selected = "selected",
}
